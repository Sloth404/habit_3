package com.moehr.habit_3.data.model

/**
 * Represents the type of habit the user wants to track.
 *
 * - [BUILD]: A positive habit to develop or increase.
 * - [BREAK]: A negative habit to reduce or eliminate.
 */
enum class HabitType {
    BUILD,
    BREAK,
}